apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helloworld
  namespace: flux-demo
spec:
  chart:
    spec:
      version: ">=0.1.0"
  test:
    enable: false
  values:
    image: "smana/helloworld:sha-f5096b2"
    replicaCount: 3
